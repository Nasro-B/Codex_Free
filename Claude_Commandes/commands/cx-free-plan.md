# /cx-free-plan

Ask the standalone Codex Free EN agent for a read-only implementation plan.

Syntax:

~~~text
/cx-free-plan [model] <request>
~~~

The result is a plan only. It does not modify files or launch the Codex GUI.
