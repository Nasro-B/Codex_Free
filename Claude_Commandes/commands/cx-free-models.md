# /cx-free-models

List the models exposed by the standalone Codex Free EN runtime, their provider and their context window.

Run:

~~~powershell
pwsh -NoProfile -File "$env:USERPROFILE\.claude\scripts\cx-free.ps1" -Mode models
~~~

The command is read-only and does not launch the Codex GUI.
